import { FooterstyleDirective } from './footerstyle.directive';

describe('FooterstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterstyleDirective();
    expect(directive).toBeTruthy();
  });
});
